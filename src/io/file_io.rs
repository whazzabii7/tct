//! # === file_io ===
//!
//! Module handles creating and reading several files.
//! used for creating and loading history files, 
//! loading and writing config,

/// handles the filesystem operattions
pub struct FileHandler {}

impl FileHandler {
    /// Default constructor of FileHandler
    pub fn init() -> Self {
        Self {}
    }
}
